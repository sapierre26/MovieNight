import { css } from "lit";

const styles = css`
  * {
    box-sizing: border-box;
    }

    body {
        line-height: 1.5;
    }

    img {
        max-width: 100%;
    }
`;

export default { styles };